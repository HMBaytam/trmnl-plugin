# Copy to .env and fill in. Required — the app fails fast on startup if missing.

# Mongo connection string (keep secret). Atlas example:
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.example.mongodb.net/?retryWrites=true&w=majority

# Database name
MONGODB=barakah
